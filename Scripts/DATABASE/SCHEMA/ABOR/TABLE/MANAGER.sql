#!jinja 

create or replace TABLE {{ database }}.ABOR.MANAGER (
	ID NUMBER(38,0) NOT NULL,
	NAME VARCHAR(16777216) NOT NULL,
	EMAIL VARCHAR(16777216),
	DEPARTMENT VARCHAR(16777216) DEFAULT 'General',
	primary key (ID)
);