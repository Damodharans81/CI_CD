#!jinja 

create or replace TABLE {{ database }}.CI_CD.SETTINGS (
	ID NUMBER(38,0) autoincrement start 1 increment 1 noorder,
	SF_USERNAME VARCHAR(16777216),
	GIT_USERID VARCHAR(16777216),
	GIT_PAT VARCHAR(16777216),
	DEFAULT_REPO VARCHAR(16777216),
	DEFAULT_BRANCH VARCHAR(16777216),
	STATUS BOOLEAN DEFAULT TRUE,
	LAST_MODIFIED TIMESTAMP_LTZ(9) DEFAULT CURRENT_TIMESTAMP()
);